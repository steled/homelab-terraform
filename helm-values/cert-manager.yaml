crds:
  enabled: true
  keep: false

config:
  apiVersion: controller.config.cert-manager.io/v1alpha1
  kind: ControllerConfiguration
  gatewayAPI:
    enabled: true

prometheus:
  enabled: true
  podmonitor:
    enabled: true
